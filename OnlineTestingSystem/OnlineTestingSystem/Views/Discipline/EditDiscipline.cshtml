@model OnlineTestingSystem.Models.Discipline
@*@model List<Discipline>*@
@*@using OnlineTestingSystem.Models*@

@{
    ViewBag.Title = "EditDiscipline";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Edit Discipline</h2>
<h4 style="font-style:oblique; font-weight:lighter;">Complete the registration form below:</h4>
<br />
<div class="container">

    @using (Html.BeginForm("EditDiscipline", "Discipline", FormMethod.Post))
    {

    <div class=" form-group">
        <label for="OldDisciplineName">Old Discipline</label>

        @*<select class="form-control" id="OldDisciplineName">

            @foreach (var discipline in Model)
            {
                <option> @discipline.DisciplineName</option>
            }
        </select>*@
        @Html.DropDownListFor(t => t.DisciplineName, ViewBag.Discipline, "Select Discipline")
        @Html.DropDownListFor(t => Model.DisciplineName, ViewBag.Discipline, "Select Discipline", new { @class = "form-control" })

        @Html.DropDownListFor(m => m.DisciplineName, ViewBag.listOfDisciplines, "Choose...", new { @class = "form-control dropdown" })



    </div>
        <div class="form-group">
            <label for="NewDisciplineName">New Discipline Name</label> <br />
            <input id="NewDisciplineName" />
        </div>
        <input class="btn btn-primary" type="submit" />

    }
    @ViewBag.message

</div>

